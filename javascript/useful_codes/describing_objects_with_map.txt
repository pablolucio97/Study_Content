const showingObject  = () => {

	const myObjects = [ 
	{
	 object: 'mouse',
	 label: 'Hyper-x',
	 category: 'gamer',
	 cost: 'U$ 39.99'
	},
	
	{
	 object: 'keyboard',
	 label: 'Hyper-x',
	 category: 'gamer',
	 cost: 'U$ 79.99'
	}
	]
	
	const object = myObjects.map(item => item.object)
	const label = myObjects.map(item => item.label)
	const category = myObjects.map(item => item.category)
	const cost = myObjects.map(item => item.cost)
	
	console.log([object, label, category, cost])

	}