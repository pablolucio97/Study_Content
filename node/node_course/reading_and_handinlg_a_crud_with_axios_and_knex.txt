1)Use the axios to create your api baseURL and export it in your 
file of read the data.

import api from './services/api'

2)Create a state for your data and type your data with typescript 
interface. Example:

interface Item{
  name: string,
  age: string
}

 const [userName, setUserName] = useState('')
  const [userAge, setUserAge] = useState('')
  const [users, setUsers] = useState<Item[]>([])
  const [userId, setUserId] = useState('')
  const [newName, setNewName] = useState('')

2)Use your methods

  //READ USERS
  useEffect(() => {
    api.get('list-users').then(response => {
      setUsers(response.data)
    })
  })

  //CREATE USERS
  const createUsers = async () => {
    
    api.post('/create-users', {
      name : userName,
      age : userAge
    }).then(response => console.log(response.data))
  }

  //ALTER USER NAME
  const alterUserName = async () => {
    api.put('/alter-users/' + userId, {name: newName})
    .then(response => setUserName(response.data))
  }

  //DELETE USER
  const deleteUser = async () => {
    api.delete('/delete-users/' + userId).then(response => response.data)
  }